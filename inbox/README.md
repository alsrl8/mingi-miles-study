# Inbox

Capture one observation per file. Use `scripts/learn capture` so filenames and
IDs remain unique across devices. Move durable explanations into `topics/`
through `scripts/learn distill`; keep the original note as learning history.
